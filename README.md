A very simple Poe API proxy server:
- required only model and messages parameter
- complete, build and send the full request to POE

Limitation:
- only support steaming output
- only support test message